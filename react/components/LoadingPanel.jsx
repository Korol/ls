import * as React from "react";

export default function () {
    return (
        <div className="loadingPanel">
            <img src={BaseUrl + "public/images/loading.svg"} />
        </div>
    );
}